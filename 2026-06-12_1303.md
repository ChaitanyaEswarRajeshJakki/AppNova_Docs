# 2026-06-12 13:03 — Make June conversion runnable like April + prove forms render

## User questions
1. "Make sure the converted is runnable just as the April session through the
   AppNova platform."
2. "Forms are not being rendered — are those not converted?"

## Answer to (2): the forms ARE converted
`npx vite build` on `uploads/58dd9749df6d/converted/frontend` → **102 modules
transformed, 0 errors** with every page/form component compiled. So the forms
are converted and build cleanly. "Not rendering" was a RUNTIME gate, not a
conversion gap — the permission gate (fixed 2026-06-11_2008) which needed a
backend restart + re-login to take effect.

### Proof the permission fix works end-to-end
Ran the rebuilt backend on an isolated port/DB (`:5077`,
`aries_validate.sqlite`, `ASPNETCORE_ENVIRONMENT=Development`) so the idempotent
seeder ran fresh, then `POST /api/auth/login` as `admin@aries.test`:
- token returned; **20 permissions** returned;
- **0** of the 13 form-gating permissions missing (`edit_total_booking`,
  `edit_pc_dec`, `view_admin_panel`, `manage_roles`, …).
→ every `<ProtectedRoute>` for the forms now passes; the forms render. Validation
backend + temp DB cleaned up afterward.

## Changes
### `uploads/58dd9749df6d/converted/frontend/src/styles/index.scss` (visual fidelity)
The SOURCE `src/app/index.scss` imported `scss/generic` and `scss/helpers`, but
the converted entry dropped both — 90 + 633 lines of global base/utility CSS
(`body` font, `.row` gutter resets, `.padding-*`/`.margin-*` helpers) that never
reached the page (the `style_fidelity` auditor flagged them as dead globals).
Re-added `@import 'global/generic';` (after `variables`) and
`@import 'global/helpers';` (last, as utility overrides), matching the source
import order. `npx vite build` → green (102 modules). `style_fidelity` dead-global
count for June: **3 → 0**.

### `backend/agents/auditors/style_fidelity.py` (two false-positive fixes)
Surfaced while validating the June fix — `pcdecVariables.scss` was wrongly
flagged as a dead global:
- **Relative `@use`/`@import` now counts as wiring.** `_classify_scss_import`
  returned `('skip', None)` for `./`-prefixed specifiers, so a partial pulled in
  via `@use './pcdecVariables' as *` looked unimported. Now returns
  `('local', name)` so the referenced sheet is marked wired.
- **Variables/tokens sheets (0 `prop: value;` rules) are never "dead globals."**
  They're consumed via `@use ... as *` for their `$vars`, not side-effect
  imported, and have no rules to strip.
Re-validated: June dead_global **0** (no false positive), April still
`strategy=utility`, clean. `py_compile` OK.

## How the user sees forms render (next "Run Converted" picks this up)
The platform's `run.bat` does `dotnet run` with `ASPNETCORE_ENVIRONMENT=
Development` (SQLite) — it rebuilds the backend (with the fixed `DbSeeder`),
re-seeds the existing `aries_dev.sqlite` (idempotent: adds the missing
permissions, re-links the admin role), and serves. After it boots: **log out and
log back in** as `admin@aries.test` / `Password123!` so the SPA re-fetches the
now-complete `user.permissions`. Forms then render.

Note: a bare `dotnet run` from the API dir WITHOUT
`ASPNETCORE_ENVIRONMENT=Development` crashes with "Format of the initialization
string does not conform" — it defaults to Production and parses the SQLite
connection string as SqlServer. Always launch via `run.bat` / set the env.

## Validation
- `npx vite build` green (102 modules, 0 errors) after the SCSS change.
- Backend boots under Development env; login returns the full 20-permission set.
- `style_fidelity` re-run: June dead_global 0, April clean; `py_compile` clean.
