# Changes — 2026-06-02 18:10

## Full codebase folder structure refactor `[DONE]`

### Phase 1 — backend/agents/ reorganised (41 flat files → 5 sub-packages)

| New package | Files moved |
|---|---|
| `backend/agents/orchestration/` | supervisor.py, runner.py, state.py, orchestrator.py, director.py |
| `backend/agents/pipeline/` | migration_pipeline.py, codegen_multipass.py, codegen_field_sync.py, planner_multipass.py, planner_polish.py, planner_field_map.py, field_extractor.py, post_codegen_browser_loop.py |
| `backend/agents/auditors/` | file_coverage.py, deploy_audit.py, parity_checker.py, round_trip_tester.py, api_contract.py, route_link_contract.py, source_routes.py, synthesize_file_map.py, diagram_qa.py, audit_run_scripts.py, line_count_fidelity.py, ui_binding.py, seed_completeness.py, report_scrubber.py |
| `backend/agents/tools/` | mermaid_renderer.py, export.py, run_manager.py, browser_test.py, scaffold.py, sample_data.py, artifact.py, legacy_screenshot.py, watch.py, demo_docs.py |
| `backend/agents/chat/` | chat.py, dev_assist.py |

`prompts.py` and `__init__.py` stay at `backend/agents/` root. `__init__.py` re-exports all public symbols for backwards compatibility. 29 files updated with new import paths.

### Phase 2 — backend/api/ structure created

- `backend/api/state.py` — all shared in-memory session dicts extracted from main.py
- `backend/api/auth.py` — auth route handlers as APIRouter
- `backend/api/middleware/http_capture.py` — HTTP capture middleware extracted from main.py
- `backend/api/routes/` — 12 route modules: analyze, chat, cost, demo, export, logs, projects, rag, review, run, session, system
- `backend/main.py` — thin entry point; mounts all 12 routers via `app.include_router()`

### Phase 3 — harness/ → evals/

- `backend/harness/` moved to `backend/evals/` (all 4 files: cli.py, eval.py, __init__.py, __main__.py)
- Old `backend/harness/` deleted
- All `backend.harness` import references updated to `backend.evals`
- README smoke tests section updated: `python -m backend.harness` → `python -m backend.evals`

### Structural changes (root)

- All old `changes_*.md` files moved into `changes/` folder
- `changes.md` moved to `changes/changes.md`
- Presentation files moved to `Presentations/`
- `changes/` folder is now the canonical location for all change logs

### Memory saved

- `feedback_changes_datetime.md` — from here on, all changes logged as `changes/YYYY-MM-DD_HHMM.md`
