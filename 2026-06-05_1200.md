# Changes — 05-06-2026 (sync from changelog)

## Converter pipeline: follow the April single-project template (stop multi-project regressions)

Applied all changes described in the 05-06-2026 changes entry that were missing from the workspace.

### Files modified

- **backend/agents/tools/scaffold.py** — new `_ensure_dotnet_appsettings()` helper wired into `ensure_build_manifests()`. Backfills `appsettings.json` / `.Development.json` / `.Production.json` beside the API `.csproj` when the agent didn't emit them. Base carries SQLite `DefaultConnection` + `UseAzureSql:false`; Development adds a random dev JWT key + verbose logging; Production flips to Azure SQL with `__PLACEHOLDER__` shapes. Idempotent.

- **backend/agents/tools/run_manager.py** — dev previews now set `ASPNETCORE_ENVIRONMENT=Development` via `env.setdefault(...)` so converted .NET apps load the Development appsettings overlay (SQLite + dev JWT + detailed errors/Swagger) instead of defaulting to Production.

- **backend/agents/prompts.py** — code-generation prompt: added `.NET BACKEND TEMPLATE — DEFAULT TO ONE PROJECT`, `DATABASE — DEFAULT TO SQLite FOR DEVELOPMENT`, `DEPENDENCY INJECTION — INTERFACE-BASED`, and `DEFINE EACH CLASS ONCE` blocks. migration-planner prompt: updated deploy_config namespace-path guidance and schema example paths from multi-project `backend/src/ARIES.*` to single-project `backend/{{display}}.API/`. code-review prompt: updated audit table example paths to single-project form. File-header comment example updated to single-project path.

- **backend/playbooks/registry.py** — laravel-to-dotnet playbook `migration-planner` hint: explicit single-project layout mandate (`backend/{display}.API/`). `code-generation` hint: added SQLite-by-default, interface-based DI, define-once/no-forward-stub rules, always-emit-appsettings. `data-migration` hint: migrations under `{display}.API/Migrations/` + DevSeeder requirement.

### Verification

- `python -m py_compile` passes on all four files.

---

## Run manager: stop misclassifying healthy multistack launches as `process_crashed`

### Files modified

- **backend/agents/tools/run_manager.py** — in the `rc==0` detached branch of `_launch_and_await_ready`: when `run._backend_port` is set AND the `launched in separate windows` success marker is present in the log tail, confirm detached success via the **backend** first (HTTP probe, falling back to plain TCP so a JWT-protected API returning 401/403 still counts as alive), then best-effort probe the frontend. For single-stack detached launchers (no backend port), the frontend probe budget grows from 8 s → 20 s when the success marker is present. A confirmed launch is classified `running_detached`, not `process_crashed`.

`python -m py_compile backend/agents/tools/run_manager.py` passes.
