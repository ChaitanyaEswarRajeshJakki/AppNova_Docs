# Changes — 2026-06-02 17:55

## Jina embeddings wired into RAG pipeline `[DONE]`

- `backend/playbooks/rag/embeddings.py` (`_embed_st`): model resolution now checks `JINA_EMBEDDINGS_MODEL_PATH` first, then `APPNOVA_RAG_ST_MODEL`, then falls back to `BAAI/bge-small-en-v1.5`
- `.env` already had `JINA_EMBEDDINGS_MODEL_PATH` set — now it is honoured
- README updated: four-tier embedding table, `JINA_EMBEDDINGS_MODEL_PATH` env var row added

---

## Complete README refactor `[DONE]`

- Corrected stale defaults: `APPNOVA_CODEGEN_CHUNK_SIZE` 50→65, `APPNOVA_CODEGEN_COOLDOWN_SECONDS` 30→15
- Added missing env vars: `APPNOVA_CODEGEN_PARALLELISM` (3), `APPNOVA_CODEGEN_MULTIPASS_THRESHOLD` (100), `APPNOVA_USERS`, `APPNOVA_PASSWORD_HASH`, `APPNOVA_SESSION_HOURS`, `PLAYWRIGHT_BROWSERS_PATH`, `CHROMA_TELEMETRY_DISABLED`
- Corrected agent section: discovery is a pre-flight pass (`agent_id=_discovery`, not in `AGENT_REGISTRY`); 13 in registry + 1 pre-flight
- Added undocumented API routes: source/converted file browser, file watcher, clone, siblings, downstream
- Claude CLI flags now in architecture diagram: `--verbose --permission-mode bypassPermissions --disallowed-tools`

---

## Codebase folder structure refactor — Phase 1, 2, 3 `[IN-PROGRESS]`

See `changes/2026-06-02_refactor_structure.md` for full plan and progress.
