# 2026-06-11 12:15 — April-style two-terminal run.bat (converted output + generator)

Goal: when "Run Converted" is clicked, AppNova should open TWO visible terminals
— one running `dotnet` (backend), one running `npm` (frontend) — with LIVE output
in each, matching the April c9afaa02f85e session. Make this automatic for all
future conversions, with no regressions.

## Current converted output (uploads/58dd9749df6d/converted)
- **run.bat**: rewritten to launch backend and frontend each in their own visible
  `cmd /k` window; `BACKEND_PORT` default 5051; no `backend.log` redirect.
- **_run_backend.cmd**: recreated WITHOUT the `> backend.log 2>&1` redirect (output
  stays live in the terminal) and now sets `ASPNETCORE_ENVIRONMENT=Development`.

## Generator (backend/agents/tools/scaffold.py) — future conversions
- **_write_split_run_scripts**: `_run_backend.cmd` no longer redirects to a log and
  sets Development env; `run.bat` drops the `BE_LOG` plumbing, default `BACKEND_PORT`
  5060 → 5051, timeout message points to the backend window. Docstring updated.
- **Nested-.NET-csproj backstop (ensure_mandatory_structure)**: now AUTHORITATIVE —
  always (re)writes run.sh/run.bat for a nested-csproj + frontend layout, so an
  older template or LLM-emitted run.bat can no longer survive and skip the April
  two-terminal pattern.
- Port-default consistency: generic `_dual_stack_run_script` + comments and the
  scaffold `vite.config.ts` proxy default 5060 → 5051.

## No-regression checks
- `audit_run_scripts` only rewrites `timeout /t` → `ping -n`; the new run.bat uses
  `ping -n`, so it is never rewritten.
- `run_manager._tail_log_file_into_run` is best-effort: a missing `backend.log`
  no-ops (no crash, no false "crashed"). Trade-off (matches April): the hub's live
  backend-log tail / auto-fix loop no longer sees backend stdout for the .NET split
  path — that output is now in the backend terminal window instead.
- `scripts/smoke/smoke_run_converted.py` does not assert on backend.log /
  _run_backend.cmd / port 5060 — unaffected.
- `py_compile` clean. End-to-end test: a deliberately-bad single-terminal run.bat is
  overwritten by ensure_mandatory_structure with the April pattern (2 windows,
  _run_backend.cmd, npm run dev, BACKEND_PORT=5051, no redirect).
