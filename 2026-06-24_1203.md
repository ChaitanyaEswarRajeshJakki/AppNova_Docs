# 2026-06-24 12:03 — Conversion fidelity audit (TotalBooking / aries)

## What
Read-only audit comparing the legacy source (`References_by_Krishna/TotalBookingAI-Input/`, Laravel+AngularJS) against the converted output (`uploads/converted/`, .NET+React) and the golden GitHub download (`TotalBookingApp-main/`). Goal: confirm a fresh AppNova run would reproduce `TotalBookingApp-main` with all functions.

No code, converted output, or pipeline was modified. Single new artifact:
- `docs/aries_conversion_fidelity_audit_2026-06-24.md` — full gap report.

## Key findings
- **Golden ≡ working copy:** 324/332 source files byte-identical; 8 trivial diffs (EF migrations, SVGs, one `.bat`). File-level "exactly like this" already holds.
- **Scope:** only PCDEC + TotalBooking modules are in the source input; other `routes.php` modules (ALPR, CrimeLab, etc.) are out of scope and correctly stubbed.
- **Backend:** ~95% endpoint coverage. Business rules (workflow 1–8, 48h expiration, 90-min judge window, locking, cite-release, sealed records, edit-logs, CJIS, JMS) all ported.
- **Frontend:** high fidelity — all booking-form sections/fields/modals/list buckets and conditional rules ported.
- **Gaps (2 HIGH / 4 MED / 2 LOW):**
  - G1 (HIGH) document generation v1+v2 template path stubbed (`NotImplementedDueToMissingTemplates`; deliberate per 2026-05-05; `DocumentService` from-scratch fallback works).
  - G2 (HIGH) per-agency field rules not wired — `GetFields` returns `[]`; no admin screen counterpart.
  - G3 (MED) agency/permission list scoping dropped (2026-05-11).
  - G4 (MED) counties/cities cascade params ignored ("no FK column").
  - G5 (MED) role/agency attach-detach endpoints lack `manage_users`.
  - G6 (MED) sealed-records filter silently disables if table missing.
  - G7/G8 (LOW) minor lookups to verify; phantom juvenile school/grade fields.
- **Re-conversion risks:** `context/file_map.json` not shipped (FILE_COVERAGE self-audit shows false 0%); document templates external; G2/G3/G4 live only as code comments, not eval-pinned.

## Recommended next turn (not done here)
Encode the audit as backstops: ship `file_map.json`; add a conversion-parity auditor + eval gate seeded from the inventory; decide G1 policy explicitly; wire or SKIP G2; route G5/G6 to security auditor.
