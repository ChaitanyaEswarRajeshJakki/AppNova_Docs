# 2026-06-09 13:20 — File Migration Map: honest actions, self-heal, planner-map recovery

## Problem reported
Migration-planner report card's **File Migration Map** showed **all 345 source files as `DELETE`** with target `(none)`. Separately, code-generation hit **`source-coverage 4.1%` (floor 70%)** — "326 of 340 source files have no target counterpart".

## Root cause (investigated, not assumed)
One underlying failure, two symptoms. For session `3dfca6fbea95`:

1. **The migration-planner actually succeeded** — `source/context/migration-planner_attempt1.md` contains a complete, valid `file_map.json` block (`meta` + **335 mappings** with real targets). The supervisor's own `_extract_file_map_json` parses it cleanly.
2. **That JSON was never promoted to `context/file_map.json`** — the body/quality validator rejected the prose (attempt1 draft persisted → `needs_repair`), the run went into the repair/multipass path, and the valid map was lost before the persist at `supervisor.py:1863` (interrupted run / swallowed exception at `:1904`). `source/context/` had every artifact *except* `file_map.json`.
3. **The disk-walk synthesizer masked it** — with no `file_map.json` and a near-empty `converted/` (18 skeleton files written 14 min *after* the synth ran), it paired every legacy file with nothing → 345 target-less rows.
4. **The endpoint mislabeled target-less as `DELETE`** — `not targets → DELETE` conflated "no target assigned yet" with "delete this file".
5. **Code-generation got the empty fallback map** → produced only an 18-file scaffold → 4.1% coverage. Same missing-map fact drove both symptoms.

## Changes

### C — Honest action labels (`UNMAPPED` ≠ `DELETE`)
- `backend/main.py` `get_session_file_map`: `DELETE` now only when `kind` explicitly says skip/drop/delete. A target-less row **with a source** is `UNMAPPED`; target-less with no source is `CREATE`. Added `unmapped` to counts.
- `backend/main.py` `_file_map_to_markdown_table` (`_action_for`): mirrored the same derivation; added `UNMAPPED` to the export summary counts.
- `frontend/app.js` `renderFileMapTable`: added `unmapped` count chip + `UNMAPPED` filter button.
- `frontend/style.css`: amber `.chip-lite.unmapped` and `.action-chip.unmapped`.

### A — Don't freeze a delete-only map early
- `backend/agents/auditors/synthesize_file_map.py` `synthesize_and_persist`: when a fresh synth has **zero rows with targets** (`converted/` empty / nothing paired — code-gen unfinished), serve it transiently (`_transient`) instead of persisting a "delete everything" map that can never self-heal.

### B — Self-heal stale synthesized maps
- `synthesize_and_persist`: when the existing file is `_synthesized` AND target-less AND a fresh disk walk now finds targets, **overwrite** rather than augment (the augment path can never upgrade target-less rows). Planner-authored maps are never overwritten.
- `backend/main.py` `_maybe_heal_synthesized_file_map`: new helper; wired into `_load_file_map_for_review` and the `/file-map` endpoint.

### Fallback hardening (the real fix) — recover the planner map from markdown
- `backend/main.py` `_recover_planner_file_map`: before disk-synthesizing, extract the planner's `file_map` JSON from `migration-planner_attempt1.md` / `upstream_migration-planner.md` / `planner_staging/A_4.md` and persist it to the authoritative `source/context/file_map.json`. A valid planner map always beats a stem-matched disk guess.
- Wired into: `_maybe_heal_synthesized_file_map` (preferred over disk re-synth), the `_load_file_map_for_review` no-file branch, and the `/file-map` endpoint's 404 branch.

### Docs
- `README.md`: documented the markdown-recovery fallback and the `UNMAPPED` action state under the `file_map.json` contract bullet.

## Verification
- `scripts/smoke/smoke_file_map_render.py` — ALL PASSED (DELETE fixtures use explicit skip kinds, still `DELETE`).
- Real session `3dfca6fbea95`: on-read recovery turned the synthesized 345-all-DELETE map into the planner's **335 real mappings** (written to `source/context/file_map.json`). Endpoint now derives **326 REWRITE + 9 DELETE** (was 345 DELETE).
- `backend/main.py` + `synthesize_file_map.py` parse + import clean.

## Still open (not in scope of this change)
- The **15.4% coverage error remains valid**: the recovered map is the *plan* (326 files to convert); code-generation only ever wrote 18 skeleton files, so the planned targets don't exist on disk yet. **Re-run code-generation** against the now-recovered `file_map.json` to close the gap (no planner re-run needed — its map is recovered).
- Optional hardening declined this round: persist `file_map.json` in the supervisor as soon as extraction succeeds (before repair/multipass), so an interrupted repair can't drop a valid map at the source.
