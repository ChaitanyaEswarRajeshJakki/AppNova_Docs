# 2026-06-11 11:45 — Close April parity gaps in current converted output

Scope: `uploads/58dd9749df6d/converted/backend/Aries.API` (generated demo output; gitignored).
Goal: bring the current 1-to-1 conversion in line with the April reference
conversion's (`c9afaa02f85e`) production-ready setup, without breaking the
working local SQLite demo.

## Changes
- **EF migrations + provider-branch startup** (`Program.cs`): startup now branches
  on the actual EF provider — SQL Server (Azure prod) runs `db.Database.MigrateAsync()`;
  SQLite (local dev) keeps `EnsureCreated` + `DbSeeder`. Mirrors April's pattern.
- **Initial migration** (`Migrations/20260611061413_InitialCreate.*`,
  `AriesDbContextModelSnapshot.cs`): generated SQL Server-targeted DDL
  (403 nvarchar, 193 datetime2) so the Azure SQL `Migrate()` path creates the schema.
- **Design-time factory** (`Data/DesignTimeDbContextFactory.cs`, new): forces the
  SqlServer provider for `dotnet ef` only; runtime still uses DI (SQLite dev / SQL prod).
- **Extra lookups** (`Data/DbSeeder.cs`): seed counties, cities, address directions,
  address suffixes, address units (drive `address-input-lists`). Idempotent.
- **Config parity** (`appsettings.json`): add missing `Cors:AllowedOrigins`
  (`http://localhost:5050` — Program.cs reads it; was empty), plus April parity keys
  (`Jwt:ExpiryMinutes`, `LockTime`, `Integrations`, `Email`, `Serilog`,
  `ConnectionStrings:AzureSql`). Kept existing `Jwt:Secret`.

## Verification
- `dotnet build` → 0 errors.
- Fresh SQLite run: startup used EnsureCreated+seed (not Migrate); no exceptions.
- `POST /api/auth/login` (admin@aries.test) → 200 + JWT.
- `GET /api/total-bookings` → 200; `/api/total-bookings/1` → 200;
  `/api/total-bookings/address-input-lists` → 200 with seeded address data.
- Migration confirmed SqlServer-targeted and compiles.
