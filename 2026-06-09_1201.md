# Code-Review Pass — 2026-06-09

## Backend compile fixes (P0)

- `backend/Aries.API/Controllers/TotalBookingController.cs` — restored broken `CurrentUserId =>` expression-body property (scrubber had replaced it with `Current__DB_ADMIN_USER__` with no `=>`, causing CS0518 compile error)
- `backend/Aries.API/Controllers/PCDEC/PcDeclarationController.cs` — same broken property fix
- `backend/Aries.API/Repositories/UserRepository.cs` — replaced five `__SECRET_KEYVAULT_REF__` / `__DB_ADMIN_USER__` placeholders with real C# (`BCrypt.HashPassword`, `UserId = user.Id`, ownership check); fixed `int?` Enabled comparisons from `== false/true` → `== 0/1`; added `using BCrypt.Net;`
- `backend/Aries.API/Models/UserHistory.cs` — created (model referenced by UserRepository but not generated)
- `backend/Aries.API/Data/AriesDbContext.cs` — added `UserHistories` DbSet

## Backend startup fixes (P0)

- `backend/Aries.API/Program.cs` — fixed `Jwt:Key` → `Jwt:Secret` (key name in config was wrong, threw `InvalidOperationException` at startup); added conditional `UseSqlite` vs `UseSqlServer` driven by `UseAzureSql` flag
- `backend/Aries.API/Aries.API.csproj` — added `Microsoft.EntityFrameworkCore.Sqlite 8.0.0` NuGet reference

## Missing controllers created

- `backend/Aries.API/Controllers/AuthController.cs` — `POST /api/auth/login` + `GET /api/current_user`; frontend called these; no C# equivalent existed
- `backend/Aries.API/Controllers/UsersController.cs` — `GET/POST/PUT/DELETE api/users` + nested `roles` / `agencies` / `generate_documents` sub-routes
- `backend/Aries.API/Controllers/RolesController.cs` — `GET/POST/PUT/DELETE api/roles`
- `backend/Aries.API/Controllers/PermissionsController.cs` — `GET api/permissions`

## Frontend URL contract fixes (P1)

- `frontend/src/hooks/useTotalBooking.ts` — changed all `/api/total-booking` → `/api/total-bookings` (backend route is plural)
- `frontend/src/hooks/usePcdec.ts` — changed all `/api/pcdec/pc-declarations` → `/api/pc-declarations` (no `pcdec/` namespace on backend); fixed `getCount` URL from `/api/pc-declarations-count` → `/api/pc-declarations/count`; changed `getAll` path param to query param `?dashboard=`
- `backend/Aries.API/Controllers/PCDEC/PcDeclarationController.cs` — added `[HttpGet("count")]` action; updated `Index` to accept `[FromQuery] string? dashboard`
- `backend/Aries.API/Services/PCDEC/PcDeclarationService.cs` — updated `GetListAsync` interface + implementation signatures to accept `string? dashboard = null`

## Frontend routing fixes (P1)

- `frontend/src/router/AppRouter.tsx` — corrected case-sensitive lazy import paths (`login` → `Login`, `dashboard` → `Dashboard`, `admin` → `Admin`, `pcdec` → `PCDEC`); added React Router v6 future flags to `<BrowserRouter>`

## Audit artifacts created

- `docs/FILE_MAP_AUDIT.md`
- `docs/LOOKUPS_AUDIT.md`
- `docs/API_CONTRACT_AUDIT.md`
- `docs/DERIVED_TARGETS.md`
