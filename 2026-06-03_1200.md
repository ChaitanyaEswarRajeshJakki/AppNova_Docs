# Changes — 2026-06-03 12:00

## 1. README: agent count corrected (14 → 13)
- `README.md` line 26: "14 specialist agents" → "13 specialist agents"; removed `discovery` from the inline list and added a note that it is a Wave 0 pre-step, not a registry agent.
- Note: the "12-step loop" mention at line 116 is in the old-vs-new architecture comparison table and refers to the deprecated sequential runner — it is not an agent count and was left as-is.

## 2. `.env.example` added
- New file at repo root listing every supported env var with its default value and a one-line description.
- Covers: auth, server, models, timeouts, codegen tuning, planner tuning, runner mode, quality gates, post-generation features, RAG, governance/guardrails, versioning.

## 3. `CLAUDE.md` added
- New file at repo root — AI coding-assistant context for Claude Code.
- Documents stack, full directory layout, AGENT_REGISTRY wave table, key invariants (do not break), and coding conventions.

## 4. `backend/governance/` layer — new
```
backend/governance/
├── __init__.py                       exports InputSanitizer, OutputScrubber, AuditLog
├── guardrails/
│   ├── __init__.py
│   ├── policy.py                     YAML policy loader (lru_cache, fallback to built-in defaults)
│   ├── input_sanitizer.py            upload-time checks: blocked ext, file/total size, path traversal, prompt injection
│   └── output_scrubber.py            PII + secret redaction before agent reports are saved
├── audit/
│   ├── __init__.py
│   └── audit_log.py                  append-only JSONL audit trail (logs/audit.jsonl)
└── policies/
    ├── __init__.py
    └── default_policy.yaml           human-readable policy config (all guardrails configurable here)
```

### input_sanitizer highlights
- Blocks: `.exe`, `.dll`, `.so`, `.dylib`, `.bin`, `.obj`, `.o`, `.a`, `.lib`, `.pyd`
- Blocks: path traversal (`../`, `..\`)
- Warns (does not block): prompt-injection patterns (`ignore previous instructions`, `<|im_start|>`, `[INST]`, etc.)
- Per-file limit: 10 MB; session total limit: 500 MB (both configurable in policy YAML)

### output_scrubber highlights
- Redacts secrets: Anthropic/OpenAI keys, AWS access keys, PEM private-key headers, connection strings, basic-auth URLs, generic `api_key=...` patterns
- Redacts PII: email addresses, E.164 phone numbers, US SSNs, private IPv4 ranges
- Mode: `placeholder` by default — replaces match with `[REDACTED:<TYPE>]`; can be set to `remove`

### audit_log highlights
- Thread-safe append-only writes to `logs/audit.jsonl`
- Events: `auth_login`, `auth_failure`, `upload`, `session_start`, `session_end`, `agent_start`, `agent_end`, `agent_error`, `guardrail_violation`
- Never raises — a broken log cannot crash an analysis run

## 5. `backend/main.py` — governance wired in
- Added import: `from backend.governance import InputSanitizer, OutputScrubber, AuditLog`
- Module-level singletons: `_input_sanitizer`, `_output_scrubber`
- `auth_login`: `AuditLog.auth_login()` on success and failure
- `/api/upload`: files now buffered first → `InputSanitizer.check()` → HTTP 400 if blocking violation → `AuditLog.upload()` after success
- `analyze` inner function: `AuditLog.session_start()` after applicable agents are resolved
- `_track_running`: `AuditLog.agent_start()` on `agent_start` events
- `_save_partial`: `OutputScrubber.scrub()` on agent result text before persisting; `AuditLog.agent_end()` / `AuditLog.agent_error()` on completion
- `finally` block: `AuditLog.session_end()` when run finishes

## 6. README updated
- Added governance layer to the feature bullet list.
