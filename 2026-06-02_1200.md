# Changes — 2026-06-02 12:00

## Structural Refactor: RAG, Scripts, Backend Core

### 1. backend/playbooks/rag/ — RAG subpackage restructured

Reorganised flat module layout into purpose-named subfolders matching the standard RAG project pattern:

```text
backend/playbooks/rag/
├── __init__.py          (updated — now imports from subpackages)
├── chunking/
│   ├── __init__.py      (re-exports walk_and_chunk, chunk_file, Chunk)
│   └── chunker.py       (moved from rag/chunker.py)
├── embeddings/
│   ├── __init__.py      (re-exports embed_texts, provider_info)
│   └── embedder.py      (moved from rag/embeddings.py)
├── vectordb/
│   ├── __init__.py      (re-exports is_available, collection_for, rag_status, etc.)
│   └── vector_store.py  (moved from rag/client.py; fixed parents[3]→parents[4])
├── ingestion/
│   ├── __init__.py      (re-exports ingest_source, clear_session_source, ingest_result)
│   ├── ingest_source.py (moved from rag/ingest_source.py; updated imports)
│   └── ingest_result.py (moved from rag/ingest_result.py; updated imports)
├── retrieval/
│   ├── __init__.py      (re-exports retrieve, format_hits_for_prompt, context_for_agent, Hit)
│   └── retriever.py     (moved from rag/retrieve.py; updated imports)
├── utils/
│   ├── __init__.py      (re-exports seed_curated, seed_all)
│   └── seed.py          (moved from rag/seed.py; fixed parent.parent→parent.parent.parent for _EXAMPLES_DIR)
├── tests/
│   ├── __init__.py
│   └── eval.py          (moved from rag/eval.py; updated import from retrieval subpackage)
├── prompts/
│   └── __init__.py      (placeholder for prompt templates)
└── llm/
    └── __init__.py      (placeholder for LLM call helpers)
```

**External imports updated:**

- `backend/agents/prompts.py:2438` — `from backend.playbooks.rag.retrieve import` → `from backend.playbooks.rag.retrieval import`

**All external code using `from backend.playbooks.rag import X` is unaffected** — the main `__init__.py` re-exports the full public API.

### 2. scripts/ — Reorganised into subfolders

```text
scripts/
├── __init__.py
├── smoke/       — 13 smoke test scripts (smoke_*.py)
├── demos/       — 4 demo management scripts (freeze/list/load/regenerate_demo.py)
└── pipeline/    — run_migration_pipeline.py
```

- Moved 18 flat scripts into `smoke/`, `demos/`, `pipeline/` subdirectories
- Fixed `sys.path` depth in every moved script: `parent.parent` / `parents[1]` → `parents[2]`
- Updated invocation module paths in script docstrings (e.g. `scripts.smoke_mermaid` → `scripts.smoke.smoke_mermaid`)
- Added `__init__.py` to each subfolder

### 3. backend/core/ — New core subpackage

Moved 11 root-level backend modules out of `backend/` root into `backend/core/`:

| Moved | From | To |
| --- | --- | --- |
| config.py | backend/config.py | backend/core/config.py |
| auth.py | backend/auth.py | backend/core/auth.py |
| cost_tracker.py | backend/cost_tracker.py | backend/core/cost_tracker.py |
| analysis_cache.py | backend/analysis_cache.py | backend/core/analysis_cache.py |
| demo_session.py | backend/demo_session.py | backend/core/demo_session.py |
| projects.py | backend/projects.py | backend/core/projects.py |
| review.py | backend/review.py | backend/core/review.py |
| task_planner.py | backend/task_planner.py | backend/core/task_planner.py |
| prompt_enricher.py | backend/prompt_enricher.py | backend/core/prompt_enricher.py |
| session_plans.py | backend/session_plans.py | backend/core/session_plans.py |
| dev_chat.py | backend/dev_chat.py | backend/core/dev_chat.py |
| model_pricing.yaml | backend/model_pricing.yaml | backend/core/model_pricing.yaml |

**Path fixes in moved files:**

- `core/cost_tracker.py` — `parent.parent` → `parents[2]` for `_ROOT`; `_PRICING_PATH` updated to `backend/core/model_pricing.yaml`
- `core/demo_session.py` — `parent.parent` → `parents[2]` for `repo_root`; `config.py` path updated to `backend/core/config.py`

**Import updates across 17 files:**
`run_server.py`, `scripts/demos/*.py`, `scripts/smoke/smoke_required_upstream.py`, `backend/main.py`, `backend/agents/prompts.py`, `backend/agents/chat/dev_assist.py`, `backend/agents/orchestration/{orchestrator,supervisor,director,runner}.py`, `backend/api/auth.py`, `backend/evals/eval.py`, `backend/playbooks/__init__.py`, `backend/playbooks/schema.py`

All `from backend.X import` and `from backend import X` patterns updated to `from backend.core.X import` / `from backend.core import X`.

### 4. README.md fully updated

- All `backend/config.py` links → `backend/core/config.py`
- `backend/model_pricing.yaml` refs → `backend/core/model_pricing.yaml`
- `backend/playbooks/rag/chunker.py` link → `backend/playbooks/rag/chunking/chunker.py`
- Repository layout block: root-level files replaced with `core/` subpackage tree
- RAG layout block: flat files replaced with subpackage tree (vectordb/, embeddings/, chunking/, ingestion/, retrieval/, utils/, tests/, prompts/, llm/)
- Scripts layout block: flat files replaced with smoke/, demos/, pipeline/ subfolders
- Smoke test invocation paths updated from `scripts.smoke_*` to `scripts.smoke.smoke_*`
