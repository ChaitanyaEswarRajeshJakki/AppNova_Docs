# Changes — 01 July 2026

## feat(playbooks): expand the registry from 3 → 29 playbooks (broad source→target coverage)

**What**
- `backend/playbooks/registry.py` now registers **29 `PlaybookDefinition`s** (plus `GENERIC_PLAYBOOK`), up from 3. No agent, supervisor, schema, or UI change was needed — the registry docstring's contract holds ("add a `PlaybookDefinition` to `PLAYBOOK_REGISTRY`; nothing else changes"), because playbook prose is injected into agent prompts via `prompt_context_for()`.
- **Seven hand-written additions** (full verbose definitions, modeled on the flagships): `vue-to-react`, `angular2-to-react`, `django-to-fastapi`, `rails-to-nestjs`, `jsp-struts-to-springboot`, `webforms-to-blazor`, `jquery-to-react`.
- **Extended catalog (19 more) via a new `_make_playbook(...)` factory** so the file stays readable while each entry remains a complete six-layer definition:
  - Frontend → React/Blazor: `knockout-to-react`, `backbone-to-react`, `ember-to-react`, `gwt-to-react`, `flex-to-react`, `silverlight-to-blazor`.
  - Backend: `symfony-to-dotnet`, `codeigniter-to-dotnet`, `classic-asp-to-dotnet`, `aspnet-mvc-to-dotnet8`, `wcf-to-aspnetcore`, `javaee-to-springboot`, `express-to-nestjs`, `flask-to-fastapi`.
  - Legacy desktop/mainframe: `coldfusion-to-dotnet`, `vb6-to-dotnet`, `cobol-to-java`.
  - Database: `oracle-to-postgres`, `sqlserver-to-azuresql`.
- Each carries: collision-safe `source_signals` (incl. the `.scss/.sass/.less/.css` styling tier where a UI exists), idiom `synonym_pairs` (fed to the parity checker), a `prompt_preamble` naming both stacks + the idiomatic rules, per-agent `agent_hints` (at least `migration-planner` + `code-generation`, plus role-specific ones), validation floors, `skip_agent_ids` (frontend-only pairs skip `data-migration`; service/DB-only pairs skip `ui-ux`), and RAG enabled. Introduced two descriptive `codegen_style` routing tags (`python-fastapi`, `node-nest`) — the field is a free tag; nothing hard-switches on it.
- Registry merge guards against duplicate ids (raises `ValueError`).
- Updated the docs to match: Complete Guide §13.4 (full grouped catalog + "29 playbooks"), §13.5 (reframed as the extension pattern + the two backstops: generic fallback and Director mode), Build Guide §6.4, ELI5 recipe section, Runtime Prompts §4.4. All four `.html`/`.docx`/`.pdf` regenerated.

**Why**
- Requested: cover "all known sources and targets," not just the original three (later ten). The playbook layer only supplies SOURCE idioms + a recommended target mapping — the actual target is still user-chosen via the 4 dropdowns — so broadening the catalog widens auto-detection and idiomatic conversion without constraining targets. Signals were deliberately kept collision-safe across same-language families (PHP: Laravel `artisan` vs Symfony `symfony.lock`/`bin/console` vs CodeIgniter `system/CodeIgniter.php`; .NET: WebForms `*.aspx` vs MVC `App_Start/**`+`*.cshtml` vs WCF `*.svc`; Python: Django `manage.py` vs Flask `app.py`).
- **Verified:** `import backend.playbooks.registry` succeeds; `len(PLAYBOOK_REGISTRY) == 29`; every definition is frozen/hashable and its helpers (`prompt_context_for`, `active_agent_ids`) run; a 29-case detection smoke test (one representative tree per stack) resolves **each to its own playbook with zero mis-classification**, and the original three (Laravel/AngularJS/React) are not cannibalised by the additions.

**Files**
- `backend/playbooks/registry.py` (7 verbose new playbooks + `_make_playbook` factory + 19-entry `_EXTENDED_PLAYBOOKS` + registry merge)
- `AppNova_Doc_Build/AppNova_Complete_Guide.md` · `AppNova_Build_Guide.md` · `AppNova_ELI5_Guide.md` · `AppNova_Runtime_Prompts.md` (+ regenerated `.html`/`.docx`/`.pdf` for all four)

## docs(AppNova_Doc_Build): add billing/licensing, playbook deep-dive, agent-flow display/review, and agent-operations write-ups

**What**
- Expanded the four documentation guides in `AppNova_Doc_Build/` per a stakeholder request. **Additive only** — no existing content was removed; four new major sections were appended to the Complete Guide, and targeted sections were added to the ELI5, Build, and Runtime-Prompts guides. All `.html`/`.docx`/`.pdf` regenerated via `convert.py`.
- **AppNova_Complete_Guide.md** — four new production-quality sections (TOC updated; Glossary renumbered to §16, anchor `#12-glossary` preserved so existing links still work):
  - **§12 Commercial model — licensing & how Clients are billed.** Documents that the platform runs on an **Anthropic Max subscription** (flat per-seat), not metered API keys, and draws the crucial distinction between the operator's real (fixed) marginal cost and the *notional* per-token USD the `cost_tracker` reports from `model_pricing.yaml` (a showback/reference figure, not an Anthropic invoice). Includes the current Claude model/caching reference-rate table (grounded via the `claude-api` skill: Opus 4.8 $5/$25, Sonnet 5 $3/$15, Haiku 4.5 $1/$5; cache read ~10%, write 1.25×/2×), why Max fits a migration platform, four client-billing models (fixed-bid / per-app / per-seat / T&M+showback) mapped to AppNova telemetry, fair-use/rate levers (TPM throttle, concurrency, model pins, analysis cache), and platform-licensing options.
  - **§13 Playbook deep-dive.** Formal reference for the 6 immutable layers, the `resolve_playbook` scoring algorithm, full contents of the three shipped playbooks (Laravel→.NET+React, AngularJS→React, React-upgrade) with signals/type-maps/synonyms/hints/floors, how the set is **not limited** (a frontend/backend source→target matrix + a full `PlaybookDefinition` skeleton for Vue→React and how to add Django→FastAPI, Rails→NestJS, JSP/Struts→Spring Boot, WebForms→Blazor, jQuery→React), how Claude "detects and explains" the playbook at runtime (`prompt_context_for` → `PLAYBOOK GUIDANCE` injection), and RAG/validation/workflow interaction.
  - **§14 Agent-flow runs — live display & reviewable output.** The SSE event model (`wave_start`/`agent_start`/`agent_event`/`scaffold_warning`/`agent_complete`/`stream_end`), the 2000-event ring buffer + `events?since=N` reconnect + rehydration (loss-proof), the live workspace rendering (cards, streaming text, chips, waves, cost chip, logs drawer), and the full review surface (results, exports, Review-page diff/verdicts/Ready-to-Run gate, per-card chat with snapshots, deterministic audit docs, browser-test evidence, cost workbook).
  - **§15 Selecting, sequencing & operating agents.** The 14-agent roster + DAG/waves, six selection/sequencing mechanisms (Run All, Task Planner, Run-Selected, Resume, playbook skips, Director/Orchestrator modes), operating controls, recommended recipes (full-stack / frontend-only / assessment-only), and the human review gate as the final step.
  - Eight new glossary entries (Max subscription, notional/reference cost, showback/chargeback, playbook resolution, agent hint, SSE event stream, Ready-to-Run gate).
- **AppNova_ELI5_Guide.md** — added plain-language "Step 11 — Who pays, and how (the money bit)" (buffet-ticket analogy for the Max subscription and notional cost), "A closer look at the recipe books (playbooks)" (auto-detection + not-limited-to-three), and four glossary rows.
- **AppNova_Build_Guide.md** — added "Adding a playbook for ANY source→target" under §6.4 with a reusable regeneration prompt and cross-links to the Complete Guide §13 and Runtime Prompts §4.
- **AppNova_Runtime_Prompts.md** — added §4 "Playbook-injected prompt fragments (from `registry.py`)": verbatim `prompt_preamble` + per-agent `agent_hints` for all three playbooks (hints fenced as `text` so glob asterisks render faithfully) plus a runtime-assembly note.

**Why**
- The docs lacked any commercial/licensing write-up and under-documented the playbook system, the live agent-flow display, and how agents are selected/sequenced/operated — all requested for a technology-expert audience. Billing facts were grounded against the `claude-api` skill so the Max-subscription-vs-metered-API distinction is accurate and no fabricated Anthropic plan prices were introduced (the notional token USD is clearly labelled as a reference/showback figure). Playbook content was taken from ground truth in `backend/playbooks/registry.py` + `schema.py`; agent-flow/display/review and selection/sequencing content from `backend/main.py`, `supervisor.py`, `task_planner.py`, and `frontend/app.js` as already documented in the guide.
- Verified the regenerated HTML: new §12–§16 anchors/headings present in `AppNova_Complete_Guide.html`; Runtime-Prompts §4.1–4.4 present; emphasis-leak check `grep -c "Eloquent <em>"` = 0 (fenced hint blocks prevent the simple `convert.py` renderer from italicising `*Repository` / `**/*.module.scss` glob tokens).

**Files**
- `AppNova_Doc_Build/AppNova_Complete_Guide.md` (+ regenerated `.html`/`.docx`/`.pdf`)
- `AppNova_Doc_Build/AppNova_ELI5_Guide.md` (+ regenerated `.html`/`.docx`/`.pdf`)
- `AppNova_Doc_Build/AppNova_Build_Guide.md` (+ regenerated `.html`/`.docx`/`.pdf`)
- `AppNova_Doc_Build/AppNova_Runtime_Prompts.md` (+ regenerated `.html`/`.docx`/`.pdf`)
